name: Maven CI

on:
  workflow_call:
    inputs:
      publish:
        description: Publish the Maven artifact after all checks pass
        required: false
        type: boolean
        default: true
      sonar_host_url:
        description: SonarQube Server URL; leave empty for SonarQube Cloud
        required: false
        type: string
        default: ''
      sonar_organization:
        description: SonarQube Cloud organization key; leave empty for SonarQube Server
        required: false
        type: string
        default: ''
      sonar_project_key:
        description: Sonar project key; defaults to owner_repository
        required: false
        type: string
        default: ''
    secrets:
      PACKAGES_TOKEN:
        required: false
      SONAR_TOKEN:
        description: Token with Execute Analysis permission
        required: false
  workflow_dispatch:
  pull_request:
  push:

concurrency:
  group: maven-ci-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  JAVA_VERSION: '17'

jobs:
  release-validation:
    name: Validate release coordinates
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN || github.token }}
    steps:
      - name: Skip validation outside release tags
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: echo "Immutable release validation only applies to v* tags."
      - uses: actions/checkout@v4
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
      - uses: actions/setup-java@v4
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: PACKAGES_TOKEN
      - name: Reject mutable or snapshot-based releases
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        shell: bash
        run: |
          set -euo pipefail
          release_version="${GITHUB_REF_NAME#v}"
          project_version="$(mvn -B -ntp -U -Dstyle.color=never help:evaluate \
            -Dexpression=project.version -q -DforceStdout | tr -d '\r')"
          parent_version="$(mvn -B -ntp -U -Dstyle.color=never help:evaluate \
            -Dexpression=project.parent.version -q -DforceStdout | tr -d '\r')"

          if [[ "${project_version}" != "${release_version}" ]]; then
            echo "Release tag v${release_version} must match Maven project version ${project_version}."
            exit 1
          fi
          if [[ "${project_version}" == *-SNAPSHOT ]]; then
            echo "Release project version cannot be a SNAPSHOT: ${project_version}."
            exit 1
          fi
          if [[ "${parent_version}" == *-SNAPSHOT ]]; then
            echo "Release parent version cannot be a SNAPSHOT: ${parent_version}."
            exit 1
          fi

          mkdir -p target
          mvn -B -ntp -U dependency:list \
            -DincludeGroupIds=hn.shadowcore \
            -DoutputFile=target/internal-dependencies.txt \
            -DappendOutput=false
          if grep -q -- '-SNAPSHOT' target/internal-dependencies.txt; then
            echo "Release depends on mutable MercadoX SNAPSHOT artifacts:"
            grep -- '-SNAPSHOT' target/internal-dependencies.txt
            exit 1
          fi

  unit-tests:
    name: Unit tests
    needs: release-validation
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN || github.token }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: PACKAGES_TOKEN
      - name: Run unit tests
        run: >-
          mvn -B -ntp -U
          -Dtest='!*IntTest,!*IntegrationTest'
          -Dsurefire.failIfNoSpecifiedTests=false
          test
      - name: Upload unit-test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-reports
          path: target/surefire-reports/
          if-no-files-found: ignore

  integration-tests:
    name: Integration tests
    needs: release-validation
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN || github.token }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: PACKAGES_TOKEN
      - name: Run integration tests
        run: >-
          mvn -B -ntp -U
          -Dtest='*IntTest,*IntegrationTest'
          -Dsurefire.failIfNoSpecifiedTests=false
          test
      - name: Upload integration-test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-reports
          path: target/surefire-reports/
          if-no-files-found: ignore

  quality:
    name: Coverage and Sonar analysis
    needs: [unit-tests, integration-tests]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN || github.token }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ inputs.sonar_host_url || vars.SONAR_HOST_URL }}
      SONAR_ORGANIZATION: ${{ inputs.sonar_organization || vars.SONAR_ORGANIZATION }}
      SONAR_PROJECT_KEY: ${{ inputs.sonar_project_key || vars.SONAR_PROJECT_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: PACKAGES_TOKEN
      - name: Generate JaCoCo coverage report
        run: >-
          mvn -B -ntp -U clean
          org.jacoco:jacoco-maven-plugin:0.8.13:prepare-agent
          test
          org.jacoco:jacoco-maven-plugin:0.8.13:report
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-coverage-report
          path: target/site/jacoco/
          if-no-files-found: error
      - name: Analyze with SonarQube
        if: env.SONAR_TOKEN != ''
        shell: bash
        run: |
          set -euo pipefail
          project_key="${SONAR_PROJECT_KEY:-${GITHUB_REPOSITORY/\//_}}"
          sonar_args=(
            "-Dsonar.projectKey=${project_key}"
            "-Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml"
            "-Dsonar.qualitygate.wait=false"
          )
          if [[ -n "${SONAR_HOST_URL}" ]]; then
            sonar_args+=("-Dsonar.host.url=${SONAR_HOST_URL}")
          fi
          if [[ -n "${SONAR_ORGANIZATION}" ]]; then
            sonar_args+=("-Dsonar.organization=${SONAR_ORGANIZATION}")
          fi
          mvn -B -ntp -U org.sonarsource.scanner.maven:sonar-maven-plugin:5.5.0.6356:sonar "${sonar_args[@]}"

          report_file="target/sonar/report-task.txt"
          if [[ ! -s "${report_file}" ]]; then
            echo "::error title=Sonar analysis failed::Scanner did not create ${report_file}."
            exit 1
          fi

          ce_task_url="$(awk -F= '$1 == "ceTaskUrl" { print substr($0, index($0, "=") + 1) }' "${report_file}" | tr -d '\r')"
          if [[ -z "${ce_task_url}" ]]; then
            echo "::error title=Sonar analysis failed::Scanner report does not contain ceTaskUrl."
            exit 1
          fi

          analysis_id=""
          for attempt in {1..30}; do
            if ! task_json="$(curl --fail --silent --show-error \
                    --header "Authorization: Bearer ${SONAR_TOKEN}" "${ce_task_url}")"; then
              echo "::error title=Sonar analysis failed::Could not read Sonar background-task status."
              exit 1
            fi
            task_status="$(jq -r '.task.status // "UNKNOWN"' <<< "${task_json}")"
            case "${task_status}" in
              SUCCESS)
                analysis_id="$(jq -r '.task.analysisId // empty' <<< "${task_json}")"
                break
                ;;
              FAILED|CANCELED)
                echo "::error title=Sonar analysis failed::Background task finished with status ${task_status}."
                exit 1
                ;;
            esac
            sleep 2
          done

          if [[ -z "${analysis_id}" ]]; then
            echo "::error title=Sonar analysis timed out::Background task did not complete within 60 seconds."
            exit 1
          fi

          server_url="$(awk -F= '$1 == "serverUrl" { print substr($0, index($0, "=") + 1) }' "${report_file}" | tr -d '\r')"
          gate_url="${server_url}/api/qualitygates/project_status?analysisId=${analysis_id}"
          if ! gate_json="$(curl --fail --silent --show-error \
                  --header "Authorization: Bearer ${SONAR_TOKEN}" "${gate_url}")"; then
            echo "::error title=Sonar quality gate failed::Could not read quality-gate status."
            exit 1
          fi
          gate_status="$(jq -r '.projectStatus.status // "UNKNOWN"' <<< "${gate_json}")"
          if [[ "${gate_status}" != "OK" ]]; then
            failed_conditions="$(jq -r '[.projectStatus.conditions[]? | select(.status != "OK") | "\(.metricKey)=\(.actualValue) (threshold \(.errorThreshold))"] | join(", ")' <<< "${gate_json}")"
            echo "::error title=Sonar quality gate failed::Status ${gate_status}. ${failed_conditions}"
            exit 1
          fi
          echo "Sonar quality gate passed for analysis ${analysis_id}."
      - name: Explain skipped Sonar analysis
        if: env.SONAR_TOKEN == ''
        run: echo "JaCoCo coverage generated; Sonar analysis skipped because SONAR_TOKEN is not configured."

  package:
    name: Package
    needs: [unit-tests, integration-tests, quality]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN || github.token }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: PACKAGES_TOKEN
      - name: Build package
        run: mvn -B -ntp -U package -DskipTests
      - name: Publish Maven artifact
        if: >-
          (inputs.publish == true || inputs.publish == '') && github.event_name == 'push' &&
          (github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main' ||
          github.ref == 'refs/heads/master' ||
          startsWith(github.ref, 'refs/tags/v'))
        shell: bash
        run: |
          set -euo pipefail
          project_version="$(mvn -B -ntp -U -Dstyle.color=never help:evaluate \
            -Dexpression=project.version -q -DforceStdout | tr -d '\r')"

          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            mvn -B -ntp -U deploy -DskipTests
          elif [[ "${project_version}" == *-SNAPSHOT ]]; then
            mvn -B -ntp -U deploy -DskipTests
          else
            echo "Not republishing immutable version ${project_version} from a branch."
          fi
